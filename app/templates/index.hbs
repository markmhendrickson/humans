{{#if session.authenticated}}
  {{store-indicator}}
  {{edit-profile-welcome}}
  {{cover-section human=session.human editable=true}}
  {{overview-section human=session.human editable=true richEditor=true}}
{{else}}
  {{app-intro-section}}
{{/if}}